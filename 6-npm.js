// npm -                Es un comando global que viene no node
// npm --version        Lo mismo

// Instalar como dependencia local, solo para ese projecto particular:
// npm i <Nombre del Paquete>

// Instalar como dependencia global, para cualquier proyecto:
// npm install -g <Nombre del Paquete>

//  Lo mas habitual es instalar como dependencia local

// Hace falta un package.json (guarda info importante del paquete y del proyecto)
// Crear manual     ->  Se crea en la raiz, se crean las propiedades etc a mano
// npm init         ->  Se crea paso a paso. Skip con ENTER
// npm init -y      ->  Todo default